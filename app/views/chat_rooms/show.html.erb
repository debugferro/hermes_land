<div class="container">
  <div class="card-chat card-responsive">
    <div class="card-chatinfo">
      <div class="bubble-link">
      <%= link_to "<", chat_rooms_path, class: "btn-back"%>
      </div>
      <% @participants.each do |participant| %>
      <div class="avatar-bubble">

          <p><%= participant.username[0] %></p>

      </div>
      <% end %>
    </div>
    <div id="messages" class="message-scroll" data-chatroom-id="<%= @chat_room.id %>">
    <% @messages.each do |message| %>
      <%= render 'messages/message', message: message %>
    <% end %>
    </div>
    <div class="box-form">
      <%= simple_form_for @new_message, url: chat_room_messages_path(@chat_room), remote: true, html: { class: 'chatroom' } do |f| %>
        <%= f.input :content, as: :text, input_html: { class: 'chat-form' } %>
        <%= f.submit "SEND" %>
      <% end %>
    </div>
  </div>
</div>
